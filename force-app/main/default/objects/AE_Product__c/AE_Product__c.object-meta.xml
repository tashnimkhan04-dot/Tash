//NEW: Handle "Sent/Faxed to Safety New" - Console Subtab with Parent+Inquiry IDs + Validation
String currentInqId = (c != null && c.Id != null) ? String.escapeSingleQuotes(String.valueOf(c.Id)) : '';
String currentCaseNum = (c != null && c.MICC_CaseNumber__c != null) ? String.escapeSingleQuotes(c.MICC_CaseNumber__c) : '';
String parentCaseIdParam = '';

if(c != null && c.ParentId != null){
    parentCaseIdParam = String.escapeSingleQuotes(String.valueOf(c.ParentId));
}

Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  this.value = ""; ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);

CIC
Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  this.value = ""; ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);

Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  this.value = ""; ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew' + String.valueOf(counter) + '\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);

Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  this.value = ""; ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew' + String.valueOf(counter) + '\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);
if (
    typeof MICC_IS_LIGHTNING === "undefined" &&
    typeof sforce !== "undefined" &&
    sforce.console &&
    typeof sforce.console.setTabTitle === "function"
) {
    sforce.console.setTabTitle("Edit - " + "{!ParentCase.CaseNumber}");
}
function defaultTabTitle() {
    if (
        typeof MICC_IS_LIGHTNING === "undefined" &&
        typeof sforce !== "undefined" &&
        sforce.console &&
        typeof sforce.console.setTabTitle === "function"
    ) {
        sforce.console.setTabTitle("Edit - " + "{!ParentCase.CaseNumber}");
    }
}

window.onload = function() {
    defaultTabTitle();
};
var url = "{!URLFOR($Page.CCMIC_AE_CreateForm)}";
url += "?caseId=" + encodeURIComponent(parentCaseId);
url += "&potentialAE=Sent%2FFaxed+to+Safety+New";

public PageReference init() {
    String caseIdParam = ApexPages.currentPage().getParameters().get('caseId');
    if (!String.isBlank(caseIdParam)) {
        caseId = Id.valueOf(caseIdParam);
    }

    String potentialAEParam = ApexPages.currentPage().getParameters().get('potentialAE');
    String inquiryIdParam = ApexPages.currentPage().getParameters().get('inquiryId');
    if (!String.isBlank(potentialAEParam) && !String.isBlank(inquiryIdParam)) {
        try {
            update new Case(
                Id = Id.valueOf(inquiryIdParam),
                MICC_Potential_Adverse_Event__c = potentialAEParam
            );
        } catch (Exception e) {
            System.debug('Error setting Potential AE: ' + e.getMessage());
        }
    }

    return null;
}
}
if (selectedInquiry != null && selectedInquiry.Id != null) {
    selectedInquiry.MICC_Encrypt__c = true;
    update selectedInquiry;
    
    // Separate update for Potential AE to avoid field overwrite issue
    Case paeUpdate = new Case(
        Id = selectedInquiry.Id,
        MICC_Potential_Adverse_Event__c = 'Sent/Faxed to Safety New'
    );
    update paeUpdate;
}

Case paeUpdate = new Case(
    Id = selectedInquiry.Id,
    MICC_Potential_Adverse_Event__c = 'Sent/Faxed to Safety New'
);
update paeUpdate;

if (cases != null && SelectedCounter != null && 
        SelectedCounter >= 0 && SelectedCounter < cases.size()) {
        cases[SelectedCounter].MICC_Potential_Adverse_Event__c = 'Sent/Faxed to Safety New';
        update cases[SelectedCounter];
    }
Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew' + String.valueOf(counter) + '\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);

Component.Apex.actionsupport asppeNew = new Component.Apex.actionsupport();
asppeNew.event = 'onchange';
asppeNew.onSubmit =
    'var selValue = this.options[this.selectedIndex].value; ' +
    'if(selValue === "Sent/Faxed to Safety New"){ ' +
    '  var btn = document.querySelector("[id*=\'btnPAENew' + String.valueOf(counter) + '\']"); ' +
    '  if(btn){ btn.click(); } ' +
    '  return false; ' +
    '} ' +
    'return true;';
PEfld1.childcomponents.add(asppeNew);

    SELECT QualifiedApiName, DataType, Label 
FROM FieldDefinition 
WHERE EntityDefinition.QualifiedApiName = 'New_Potential_Adverse_Event__c'
AND IsNillable = false
ORDER BY QualifiedApiName ASC

            SELECT QualifiedApiName, DataType, Label, ReferenceTo
FROM FieldDefinition 
WHERE EntityDefinition.QualifiedApiName = 'New_Potential_Adverse_Event__c'
AND QualifiedApiName = 'Child_Inquiry__c'
@isTest
public static void addNewPAEToMapTest() {
    Test.startTest();
    makeData();
    
    Case parent = [SELECT Id FROM Case WHERE Subject = 'parent' LIMIT 1];
    Case child = [SELECT Id FROM Case WHERE Subject = 'child' LIMIT 1];
    
    New_Potential_Adverse_Event__c npe1 = new New_Potential_Adverse_Event__c(

            // Called on + and - oncomplete — clears ONLY new empty rows, not existing errors
function clearNumericErrors() {
    var table = document.querySelector('[id$="productTablePanel"]') || document.body;
    table.querySelectorAll('input.numeric-field').forEach(function(inp) {
        var val = (inp.value || '').trim();
        // Only clear styling if the field is now empty (new row)
        if (val === '') {
            inp.classList.remove('is-invalid', 'is-valid');
            inp.style.border = '';
            inp.style.backgroundColor = '';
            inp.style.outline = '';
            var errorEl = inp.parentNode.querySelector('.field-error');
            if (errorEl) errorEl.classList.remove('visible');
        }
        // If field has value, re-validate it to restore correct state
        else {
            validateFieldLive(inp);
        }
    });
    // Re-attach live listeners after every reRender
    attachAllNumericListeners();
}

var NUMERIC_REGEX = /^-?\d+(\.\d+)?$/;

function validateFieldLive(inp) {
    var val = (inp.value || '').trim();
    var errorEl = inp.parentNode.querySelector('.field-error');

    inp.classList.remove('is-invalid', 'is-valid');
    if (errorEl) errorEl.classList.remove('visible');

    if (val === '') return;

    if (!NUMERIC_REGEX.test(val)) {
        inp.classList.add('is-invalid');
        if (errorEl) errorEl.classList.add('visible');
    } else {
        inp.classList.add('is-valid');
        // Live clear — remove error immediately on valid input
        inp.style.outline = '';
    }
}

function attachAllNumericListeners() {
    document.querySelectorAll('input.numeric-field').forEach(function(inp) {
        // Remove old listeners by cloning
        var newInp = inp.cloneNode(true);
        inp.parentNode.replaceChild(newInp, inp);

        // Attach fresh listeners
        newInp.addEventListener('input', function() {
            validateFieldLive(newInp);
        });
        newInp.addEventListener('blur', function() {
            validateFieldLive(newInp);
        });
    });
}

// Run on page load
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', attachAllNumericListeners);
} else {
    attachAllNumericListeners();
}
        Case_PAE__c = parent.Id,
        Child_Inquiry__c = child.Id,
        Source_System__c = 'CareConnect',
        Reporting_Date__c = Date.today(),
        Entity__c = 'Novartis'
    );
    insert npe1;
    
    New_Potential_Adverse_Event__c npe2 = new New_Potential_Adverse_Event__c(
        Case_PAE__c = parent.Id,
        Child_Inquiry__c = null,
        Source_System__c = 'CareConnect',
        Reporting_Date__c = Date.today(),
        Entity__c = 'Novartis'
    );
    insert npe2;
    
    // Same Child_Inquiry__c as npe1 - triggers alreadyExists = true (lines 130-132)
    New_Potential_Adverse_Event__c npe3 = new New_Potential_Adverse_Event__c(
        Case_PAE__c = parent.Id,
        Child_Inquiry__c = child.Id,
        Source_System__c = 'CareConnect',
        Reporting_Date__c = Date.today(),
        Entity__c = 'Novartis'
    );
    insert npe3;
    
    ApexPages.StandardController sc = new ApexPages.StandardController(parent);
    MICC_InquiryViewController micc = new MICC_InquiryViewController(sc);
    Test.setCurrentPage(Page.MICC_InquiryViewPage);
    micc.SelectedCounter = 0;
    
    micc.getBuildComponent();
    
    String url = micc.ConsoleIntegrationUrl;
    System.assertNotEquals(null, url);
    
    Test.stopTest();
}
            @isTest
public static void gettersTest() {
    Test.startTest();
    makeData();
    
    Case parent = [SELECT Id FROM Case WHERE Subject = 'parent' LIMIT 1];
    ApexPages.StandardController sc = new ApexPages.StandardController(parent);
    MICC_InquiryViewController micc = new MICC_InquiryViewController(sc);
    Test.setCurrentPage(Page.MICC_InquiryViewPage);
    
    // Cover UiThemeDisplayed and ClassicBaseUrl getters
    String theme = micc.UiThemeDisplayed;
    String baseUrl = micc.ClassicBaseUrl;
    System.assertNotEquals(null, theme);
    System.assertNotEquals(null, baseUrl);
    
    Test.stopTest();
}

@isTest
public static void nullParentCaseIdTest() {
    Test.startTest();
    makeData();
    
    Case parent = [SELECT Id FROM Case WHERE Subject = 'parent' LIMIT 1];
    ApexPages.StandardController sc = new ApexPages.StandardController(parent);
    MICC_InquiryViewController micc = new MICC_InquiryViewController(sc);
    
    // Set id param to cover the null branch
    PageReference pageRef = Page.MICC_InquiryViewPage;
    pageRef.getParameters().put('id', String.valueOf(parent.Id));
    Test.setCurrentPage(pageRef);
    
    // Cover else-if branch - npe2 with null Child_Inquiry__c
    // and Case_PAE__c = parent.Id which IS in inquiryCaseIds
    New_Potential_Adverse_Event__c npe = new New_Potential_Adverse_Event__c(
        Case_PAE__c = parent.Id,
        Child_Inquiry__c = null,
        Source_System__c = 'CareConnect',
        Reporting_Date__c = Date.today(),
        Entity__c = 'Novartis'
    );
    insert npe;
    
    micc.SelectedCounter = 0;
    micc.getBuildComponent();
    System.assertNotEquals(null, micc.NewPAEMap);
    
    Test.stopTest();
}
@isTest
public class PAE_ProductInfoControllerTest {

    @testSetup
    static void setupTestData() {
        // Create test PAE record with Product Info JSON
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'srNo' => '1',
                'productName' => 'Adakveo',
                'productType' => 'Suspect',
                'indication' => 'Test Indication',
                'dose' => '10.5',
                'doseUnit' => 'mg',
                'frequency' => '2',
                'route' => 'Oral',
                'lotNumber' => 'LOT001',
                'expiryDate' => '2026-12-31',
                'therapyStartDate' => '2026-01-01',
                'therapyEndDate' => '2026-06-30',
                'pickupStatus' => 'Yes',
                'technicalComplaint' => 'No',
                'technicalComplaintNumber' => 'TC001',
                'technicalComplaintDescription' => 'Test description'
            },
            new Map<String, Object>{
                'srNo' => '2',
                'productName' => 'Test Product',
                'productType' => 'Concomitant',
                'indication' => '',
                'dose' => '',
                'doseUnit' => '',
                'frequency' => '',
                'route' => '',
                'lotNumber' => '',
                'expiryDate' => null,
                'therapyStartDate' => null,
                'therapyEndDate' => null,
                'pickupStatus' => 'Unknown',
                'technicalComplaint' => 'Unknown',
                'technicalComplaintNumber' => '',
                'technicalComplaintDescription' => ''
            }
        });
        insert pae;
    }

    @isTest
    static void testWithProductData() {
        New_Potential_Adverse_Event__c pae = [
            SELECT Id FROM New_Potential_Adverse_Event__c LIMIT 1
        ];

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        // Verify rows populated
        System.assertNotEquals(null, ctrl.productRows);
        System.assertEquals(2, ctrl.productRows.size());

        // Verify first row values
        PAE_ProductInfoController.ProductRow row1 = ctrl.productRows[0];
        System.assertEquals('1', row1.srNo);
        System.assertEquals('Adakveo', row1.productName);
        System.assertEquals('Suspect', row1.productType);
        System.assertEquals('Test Indication', row1.indication);
        System.assertEquals('10.5', row1.dose);
        System.assertEquals('mg', row1.doseUnit);
        System.assertEquals('2', row1.frequency);
        System.assertEquals('Oral', row1.route);
        System.assertEquals('LOT001', row1.lotNumber);
        System.assertEquals('2026-12-31', row1.expiryDate);
        System.assertEquals('2026-01-01', row1.therapyStartDate);
        System.assertEquals('2026-06-30', row1.therapyEndDate);
        System.assertEquals('Yes', row1.pickupStatus);
        System.assertEquals('No', row1.technicalComplaint);
        System.assertEquals('TC001', row1.technicalComplaintNumber);
        System.assertEquals('Test description', row1.technicalComplaintDescription);

        // Verify second row
        PAE_ProductInfoController.ProductRow row2 = ctrl.productRows[1];
        System.assertEquals('2', row2.srNo);
        System.assertEquals('Test Product', row2.productName);
    }

    @isTest
    static void testWithEmptyJSON() {
        // Create PAE with blank JSON
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        // Verify empty rows
        System.assertNotEquals(null, ctrl.productRows);
        System.assertEquals(0, ctrl.productRows.size());
    }

    @isTest
    static void testWithNullProductName() {
        // Create PAE where both productName and productType are null
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'srNo' => '1',
                'productName' => null,
                'productType' => null,
                'indication' => null,
                'dose' => null,
                'doseUnit' => null,
                'frequency' => null,
                'route' => null,
                'lotNumber' => null,
                'expiryDate' => null,
                'therapyStartDate' => null,
                'therapyEndDate' => null,
                'pickupStatus' => null,
                'technicalComplaint' => null,
                'technicalComplaintNumber' => null,
                'technicalComplaintDescription' => null
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        // Row with null productName and productType should be excluded
        System.assertEquals(0, ctrl.productRows.size());
    }

    @isTest
    static void testWithMissingKeys() {
        // Create PAE with minimal JSON — missing some keys
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'productName' => 'MinimalProduct',
                'productType' => 'Treatment'
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        System.assertEquals(1, ctrl.productRows.size());
        System.assertEquals('MinimalProduct', ctrl.productRows[0].productName);
        System.assertEquals('Treatment', ctrl.productRows[0].productType);
        // Missing fields should be empty string
        System.assertEquals('', ctrl.productRows[0].dose);
        System.assertEquals('', ctrl.productRows[0].doseUnit);
    }

    @isTest
    static void testSrNoFallback() {
        // Test that srNo falls back to counter when blank
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'srNo' => '',
                'productName' => 'Product A',
                'productType' => 'Suspect'
            },
            new Map<String, Object>{
                'srNo' => '',
                'productName' => 'Product B',
                'productType' => 'Concomitant'
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        System.assertEquals(2, ctrl.productRows.size());
        System.assertEquals('1', ctrl.productRows[0].srNo);
        System.assertEquals('2', ctrl.productRows[1].srNo);
    }

    @isTest
    static void testTechnicalComplaintCombination() {
        // Test technicalComplaint combining number and description when blank
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'productName' => 'Product A',
                'productType' => 'Suspect',
                'technicalComplaint' => '',
                'technicalComplaintNumber' => 'TC123',
                'technicalComplaintDescription' => 'Some description'
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        System.assertEquals(1, ctrl.productRows.size());
        System.assertEquals('TC123 - Some description', ctrl.productRows[0].technicalComplaint);
    }

    @isTest
    static void testTechnicalComplaintNumberOnly() {
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'productName' => 'Product A',
                'productType' => 'Suspect',
                'technicalComplaint' => '',
                'technicalComplaintNumber' => 'TC123',
                'technicalComplaintDescription' => ''
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        System.assertEquals('TC123', ctrl.productRows[0].technicalComplaint);
    }

    @isTest
    static void testTechnicalComplaintDescriptionOnly() {
        New_Potential_Adverse_Event__c pae = new New_Potential_Adverse_Event__c();
        pae.Product_Info_JSON__c = JSON.serialize(new List<Map<String, Object>>{
            new Map<String, Object>{
                'productName' => 'Product A',
                'productType' => 'Suspect',
                'technicalComplaint' => '',
                'technicalComplaintNumber' => '',
                'technicalComplaintDescription' => 'Only description'
            }
        });
        insert pae;

        Test.startTest();
        ApexPages.StandardController sc = new ApexPages.StandardController(pae);
        PAE_ProductInfoController ctrl = new PAE_ProductInfoController(sc);
        Test.stopTest();

        System.assertEquals('Only description', ctrl.productRows[0].technicalComplaint);
    }
}
    private class MockHttpResponse implements HttpCalloutMock {
    public HTTPResponse respond(HTTPRequest req) {
        HTTPResponse res = new HTTPResponse();
        res.setStatusCode(200);
        res.setBody('{"status":"success"}');
        return res;
    }
}
                Test.setMock(HttpCalloutMock.class, new MockHttpResponse());

               
// ----------- 3. Dose & Frequency -----------
var numericRegex = /^-?\d+(\.\d+)?$/;
var hasInvalid   = false;
var firstInvalid = null;

if (productTable) {
    productTable.querySelectorAll('tr').forEach(function(tr) {
        var tds = tr.querySelectorAll('td');
        if (tds.length < 7) return;

        var doseInput = tds[4].querySelector('input.numeric-field');
        var freqInput = tds[6].querySelector('input.numeric-field');

        [{inp: doseInput}, {inp: freqInput}].forEach(function(item) {
            var inp = item.inp;
            if (!inp) return;

            // Always reset first
            inp.classList.remove('is-invalid', 'is-valid');
            var errorEl = inp.parentNode.querySelector('.field-error');
            if (errorEl) errorEl.classList.remove('visible');

            var val = (inp.value || '').trim();

            // Empty = skip, no error
            if (val === '') return;

            if (!numericRegex.test(val)) {
                hasInvalid = true;
                inp.classList.add('is-invalid');
                if (errorEl) errorEl.classList.add('visible');
                if (!firstInvalid) firstInvalid = inp;
            } else {
                inp.classList.add('is-valid');
            }
        });
    });
}

if (hasInvalid) {
    if (firstInvalid) firstInvalid.focus();
    return false;
}

            /* Inline error text below field */
.field-error {
    display: none;
    color: #c23934;
    font-size: 10.5px;
    margin-top: 3px;
    line-height: 1.3;
    font-weight: 500;
}
.field-error.visible {
    display: block;
}

/* Invalid state */
input.is-invalid {
    border-color: #c23934 !important;
    outline: 2px solid #c23934 !important;
    background-color: #fff8f8 !important;
}

/* Valid state */
input.is-valid {
    border-color: #2e844a !important;
    outline: none !important;
    background-color: #f4fdf6 !important;
}

            <input type="text" class="numeric-field" ... />
<span class="field-error">Dose must be numeric</span>

                /* Wrapper on the td must be relative */
td.numeric-td {
    position: relative;
    padding-bottom: 2px;
}

/* Error floats BELOW input without pushing layout */
.field-error {
    display: none;
    position: absolute;
    left: 6px;
    top: 100%;
    margin-top: 1px;
    background: #fff0f0;
    border: 1px solid #c23934;
    border-radius: 2px;
    color: #c23934;
    font-size: 10px;
    font-weight: 600;
    padding: 2px 6px;
    white-space: nowrap;
    z-index: 999;
    line-height: 1.4;
    box-shadow: 0 2px 6px rgba(194,57,52,0.15);
}

.field-error.visible {
    display: block;
}

input.is-invalid {
    border: 2px solid #c23934 !important;
    outline: none !important;
    background-color: #fff8f8 !important;
}

input.is-valid {
    border: 1px solid #2e844a !important;
    outline: none !important;
    background-color: #f4fdf6 !important;
}

                <!-- Dose td -->
<td class="numeric-td">
    <input type="text" class="numeric-field" data-label="Dose" placeholder="" />
    <span class="field-error">Dose must be numeric</span>
</td>

<!-- Frequency td -->
<td class="numeric-td">
    <input type="text" class="numeric-field" data-label="Frequency" placeholder="" />
    <span class="field-error">Frequency must be numeric</span>
</td>
// After appending new TR, clear any error state on new row inputs
newTr.querySelectorAll('input.numeric-field').forEach(function(inp) {
    inp.classList.remove('is-invalid', 'is-valid');
    var errorEl = inp.parentNode.querySelector('.field-error');
    if (errorEl) errorEl.classList.remove('visible');
});

                <apex:commandLink action="{!addRow}" reRender="productTablePanel"
    styleClass="addButton" rendered="{!rows.size < 10}"
    oncomplete="clearNumericErrors();">
    <span>+</span>
</apex:commandLink>

                function clearNumericErrors() {
    var table = document.getElementById('{!$Component.productTablePanel}')
                || document.querySelector('[id$="productTablePanel"]');
    if (!table) return;

    table.querySelectorAll('input.numeric-field').forEach(function(inp) {
        inp.classList.remove('is-invalid', 'is-valid');
        inp.style.border = '';
        inp.style.backgroundColor = '';
        var errorEl = inp.parentNode.querySelector('.field-error');
        if (errorEl) errorEl.classList.remove('visible');
    });
}

      .field-error {
    display: none;
    position: absolute;
    left: 0;
    top: 100%;
    margin-top: 2px;
    background: #fff0f0;
    border: 1px solid #c23934;
    border-radius: 2px;
    color: #c23934;
    font-size: 10px;
    font-weight: 600;
    padding: 2px 5px;
    white-space: nowrap;
    width: auto;
    z-index: 9999;
    line-height: 1.4;
    pointer-events: none;
}

.field-error.visible {
    display: inline-block;   /* NOT block — this is the key */
}


                td.numeric-td {
    position: relative;
    overflow: visible !important;  /* allow error to spill out */
    padding-bottom: 2px;
}

                input.errorInput,
.unifiedTextBox.errorInput {
    border: 2px solid red !important;
}

                <td class="numeric-td">
    <apex:inputText value="{!row.dose}" 
                    styleClass="numeric-field unifiedTextBox"
                    html-data-label="Dose"
                    html-placeholder=""/>
    <span class="field-error">Dose must be numeric</span>
</td>

                <td class="numeric-td">
    <apex:inputText value="{!row.frequency}" 
                    styleClass="numeric-field unifiedTextBox"
                    html-data-label="Frequency"
                    html-placeholder=""/>
    <span class="field-error">Frequency must be numeric</span>
</td>
// Called on + and - oncomplete — clears ONLY new empty rows, not existing errors
function clearNumericErrors() {
    var table = document.querySelector('[id$="productTablePanel"]') || document.body;
    table.querySelectorAll('input.numeric-field').forEach(function(inp) {
        var val = (inp.value || '').trim();
        // Only clear styling if the field is now empty (new row)
        if (val === '') {
            inp.classList.remove('is-invalid', 'is-valid');
            inp.style.border = '';
            inp.style.backgroundColor = '';
            inp.style.outline = '';
            var errorEl = inp.parentNode.querySelector('.field-error');
            if (errorEl) errorEl.classList.remove('visible');
        }
        // If field has value, re-validate it to restore correct state
        else {
            validateFieldLive(inp);
        }
    });
    // Re-attach live listeners after every reRender
    attachAllNumericListeners();
}

var NUMERIC_REGEX = /^-?\d+(\.\d+)?$/;

function validateFieldLive(inp) {
    var val = (inp.value || '').trim();
    var errorEl = inp.parentNode.querySelector('.field-error');

    inp.classList.remove('is-invalid', 'is-valid');
    if (errorEl) errorEl.classList.remove('visible');

    if (val === '') return;

    if (!NUMERIC_REGEX.test(val)) {
        inp.classList.add('is-invalid');
        if (errorEl) errorEl.classList.add('visible');
    } else {
        inp.classList.add('is-valid');
        // Live clear — remove error immediately on valid input
        inp.style.outline = '';
    }
}

function attachAllNumericListeners() {
    document.querySelectorAll('input.numeric-field').forEach(function(inp) {
        // Remove old listeners by cloning
        var newInp = inp.cloneNode(true);
        inp.parentNode.replaceChild(newInp, inp);

        // Attach fresh listeners
        newInp.addEventListener('input', function() {
            validateFieldLive(newInp);
        });
        newInp.addEventListener('blur', function() {
            validateFieldLive(newInp);
        });
    });
}

// Run on page load
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', attachAllNumericListeners);
} else {
    attachAllNumericListeners();
}
@isTest
static void testStripHtmlTags_HtmlBodyPath() {
    Case c = createTestCase();
    
    // Update Case with SuppliedEmail to also cover line 1623
    c.SuppliedEmail = 'customer@test.com';
    update c;
    
    EmailMessage em = new EmailMessage(
        ParentId    = c.Id,
        Subject     = 'Strip HTML Test',
        HtmlBody    = '<div>Hi<br/>There</div><p>Para</p>&nbsp;&amp;&lt;&gt;',
        // DO NOT set TextBody at all — omit it entirely
        FromAddress = 'strip@test.com',
        ToAddress   = 'to@test.com',
        MessageDate = DateTime.now().addDays(-1),
        Incoming    = true,
        Status      = '3'
    );
    insert em;
    
    Test.startTest();
    Test.setCurrentPage(new PageReference('/apex/test'));
    // Use String.valueOf to ensure it's a 15/18 char Id string
    ApexPages.currentPage().getParameters().put('caseId', String.valueOf(c.Id));
    
    ApexPages.StandardController sc = 
        new ApexPages.StandardController(new New_Potential_Adverse_Event__c());
    CCMIC_AE_CreateFormController ctrl = new CCMIC_AE_CreateFormController(sc);
    Test.stopTest();
    
    System.assertNotEquals(null, ctrl);
}
}

function confirmSaveCase() {
    var lastNameInput = document.querySelector("input[id*='Reporter_Last_Name']");
    var productTable = document.querySelector('.productTable tbody');
    var firstRow = productTable ? productTable.querySelector('tr:first-child') : null;
    var productInput = firstRow ? firstRow.querySelectorAll('td')[1].querySelector('input') : null;
    var eventDescInput = document.querySelector('.fullWidthTextarea');

    var isLastNameEmpty = !lastNameInput || lastNameInput.value.trim() === '';
    var isProductEmpty = !productInput || productInput.value.trim() === '';
    var isEventDescEmpty = !eventDescInput || eventDescInput.value.trim() === '';

    // Combined message
    if (isLastNameEmpty && isProductEmpty && isEventDescEmpty) {
        alert('Please fill the required fields: Reporter Last Name, Product Name and Event Description.');
        return false;
    }
    if (isLastNameEmpty && isProductEmpty) {
        alert('Please fill the required fields: Reporter Last Name and Product Name.');
        return false;
    }
    if (isLastNameEmpty && isEventDescEmpty) {
        alert('Please fill the required fields: Reporter Last Name and Event Description.');
        return false;
    }
    if (isProductEmpty && isEventDescEmpty) {
        alert('Please fill the required fields: Product Name and Event Description.');
        return false;
    }

    // Individual with native tooltip
    if (isLastNameEmpty) {
        lastNameInput.setCustomValidity('Please enter the Reporter Last Name.');
        lastNameInput.reportValidity();
        return false;
    }
    lastNameInput.setCustomValidity('');

    if (isProductEmpty) {
        productInput.setCustomValidity('Please enter the Product name.');
        productInput.reportValidity();
        return false;
    }
    productInput.setCustomValidity('');

    if (isEventDescEmpty) {
        eventDescInput.setCustomValidity('Please enter the Event Description.');
        eventDescInput.reportValidity();
        return false;
    }
    eventDescInput.setCustomValidity('');

    return true;
}

Step 1: Get Base64 of your PDF
Open browser console (F12) and run:
jsconst input = document.createElement('input');
input.type = 'file';
input.accept = '.pdf';
input.onchange = e => {
    const reader = new FileReader();
    reader.onload = ev => {
        const b64 = ev.target.result.split('base64,')[1];
        copy(b64);
        console.log('✅ Base64 copied to clipboard!');
    };
    reader.readAsDataURL(e.target.files[0]);
};
input.click();
Pick your PDF → base64 auto copied to clipboard.

Step 2: Upload File via REST Explorer
Open Salesforce Inspector → REST Explorer
Set:

Method: POST
URL: /services/data/v59.0/sobjects/ContentVersion
Body:

json{
    "Title": "TestFile",
    "PathOnClient": "TestFile.pdf",
    "VersionData": "PASTE_BASE64_HERE",
    "IsMajorVersion": true
}
Hit Send → you'll get back a response like:
json{
    "id": "068XXXXXXXXXXXXXXX",
    "success": true
}
Copy that id — this is your ContentVersion Id.

Step 3: Get ContentDocumentId
In REST Explorer:

Method: GET
URL: /services/data/v59.0/sobjects/ContentVersion/068XXXXXXXXXXXXXXX?fields=ContentDocumentId

Response:
json{
    "ContentDocumentId": "069XXXXXXXXXXXXXXX"
}
Copy the ContentDocumentId.

Step 4: Link to Case

Method: POST
URL: /services/data/v59.0/sobjects/ContentDocumentLink
Body:

json{
    "ContentDocumentId": "069XXXXXXXXXXXXXXX",
    "LinkedEntityId": "500XXXXXXXXXXXXXXX",
    "ShareType": "V",
    "Visibility": "AllUsers"
}
Hit Send → done. Go check your Case, PDF will be in Files section.


Multiple Cases — Step by Step
Steps 1, 2, 3 are exactly the same — upload the file once and get the ContentDocumentId.
Only Step 4 changes — repeat the ContentDocumentLink POST for each Case:

Step 4a: Link to Case 1

Method: POST
URL: /services/data/v59.0/sobjects/ContentDocumentLink
Body:

json{
    "ContentDocumentId": "069XXXXXXXXXXXXXXX",
    "LinkedEntityId": "500CASE_ID_1",
    "ShareType": "V",
    "Visibility": "AllUsers"
}

Step 4b: Link to Case 2
Same POST, just change LinkedEntityId:
json{
    "ContentDocumentId": "069XXXXXXXXXXXXXXX",
    "LinkedEntityId": "500CASE_ID_2",
    "ShareType": "V",
    "Visibility": "AllUsers"
}

Only Option Now — REST Explorer
Since Execute Anonymous has a hard 32KB limit, for a 304KB PDF you have to use REST Explorer. No way around it.
Go back to the REST Explorer approach but use the Attachment object instead of ContentVersion so you get the classic preview:
Step 1: Get just the raw base64 from console:
jsconst input = document.createElement('input');
input.type = 'file';
input.accept = '.pdf';
input.onchange = e => {
    const reader = new FileReader();
    reader.onload = ev => {
        const b64 = ev.target.result.split('base64,')[1];
        navigator.clipboard.writeText(b64)
            .then(() => console.log('✅ Copied!'))
            .catch(() => console.log(b64)); // prints if clipboard fails
    };
    reader.readAsDataURL(e.target.files[0]);
};
input.click();
Step 2: REST Explorer — POST once per Case:

Method: POST
URL: /services/data/v59.0/sobjects/Attachment
Body:

json{
    "Name": "Adverse_Event_Attachment_Test.pdf",
    "Body": "PASTE_BASE64_HERE",
    "ParentId": "500CASE_ID_HERE",
    "ContentType": "application/pdf"
}
{
    "batchRequests": [
        {
            "method": "POST",
            "url": "/services/data/v59.0/sobjects/Attachment",
            "richInput": {
                "Name": "Adverse_Event_Attachment_Test.pdf",
                "Body": "PASTE_BASE64_HERE",
                "ParentId": "500CASE_ID_1",
                "ContentType": "application/pdf"
            }
        },
        {
            "method": "POST",
            "url": "/services/data/v59.0/sobjects/Attachment",
            "richInput": {
                "Name": "Adverse_Event_Attachment_Test.pdf",
                "Body": "PASTE_BASE64_HERE",
                "ParentId": "500CASE_ID_2",
                "ContentType": "application/pdf"
            }
        },
        {
            "method": "POST",
            "url": "/services/data/v59.0/sobjects/Attachment",
            "richInput": {
                "Name": "Adverse_Event_Attachment_Test.pdf",
                "Body": "PASTE_BASE64_HERE",
                "ParentId": "500CASE_ID_3",
                "ContentType": "application/pdf"
            }
        }
    ]
}
